var stringsArray = ["apple", "fatema", "sun", "date", "girl"];
var lengthsArray = [];
for (var i = 0; i < stringsArray.length; i++) {
  var length = stringsArray[i].length;
  lengthsArray.push(length);
}
console.log(lengthsArray);
